models:
  Administration/Category:
    id: string char(26) primary
    name: string unique
    parent_id: char(26) foreign:categories,id nullable
    remark: text nullable
    created_by: char(26) foreign:users,id
    updated_by: char(26) foreign:users,id nullable
    timestamps: true

controllers:
  Administration/Category:
    resource: api

seeders: Administration/Category
