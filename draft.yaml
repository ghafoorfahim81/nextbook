models:
  Account/AccountType:
    id: id
    name: string unique
    remark: text nullable 
    created_by: id foreign:users
    updated_by: id foreign:users nullable
    timestamps: true

controllers:
  Account/AccountType:
    resource: web

seeders: Account/AccountType
