models:
  Administration/Branch:
    id: id
    name: string
    is_main: boolean default:false
    parent_id: id foreign:branches,id nullable
    sub_domain: string nullable
    remark: text nullable
    created_by: id foreign:users,id
    updated_by: id foreign:users,id nullable
    timestamps: true

controllers:
  Administration/Branch:
    resource: api

seeders: Administration/Branch
