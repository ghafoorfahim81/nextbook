models:
  Administration/Store:
    id: id
    name: string unique
    address: string nullable
    is_main: boolean
    branch_id: id foreign:branches
    created_by: id foreign:users
    updated_by: id foreign:users nullable
    timestamps: true

controllers:
  Administration/Store:
    resource: api

seeders: Administration/Store
